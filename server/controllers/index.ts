import { getAllUsers, getSingleUser, createUser, loginUser, deleteUser, addSlices } from './userController';
import { getAllEntries, getLastTwentyEntries, createEntry, deleteEntry } from './SliceController';

export { getAllUsers, getSingleUser, createUser, loginUser, deleteUser, addSlices, getAllEntries, getLastTwentyEntries, createEntry, deleteEntry };

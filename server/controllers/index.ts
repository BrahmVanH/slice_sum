import { getAllUsers, getSingleUser, createUser, loginUser, deleteUser } from './userController';
import { getAllEntries, getLastTwentyEntries, createEntry, deleteEntry } from './SliceController';

export { getAllUsers, getSingleUser, createUser, loginUser, deleteUser, getAllEntries, getLastTwentyEntries, createEntry, deleteEntry };
